#include <stdio.h>
/**
 * main - program that prints all the numbers of base, followed by a new line
 * Return: Always 0
 */

int main(void)
{
char a;
int b;

a = 'c';
b = 0;
while
	(b < 10) {
		putchar(b + '0');
		b++
		}
while
	(a <= 'f') {
		putchar(a);
		a++
	}
putchar('\n');
return (0);
}
