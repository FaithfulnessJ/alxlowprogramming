#include <stdio.h>
/**
 * main - prints the alphabet in lowercase, followed by a new line
 * Return: Always 0
 */

int main(void)
{
char a;
a = 'g';
	while
		(a <= 'z') {
			if ((a != 'q' && c !=  'e') && c <= 'z'
					putchar(a);
			a++;
		}
	putchar('\n');
return (0);
}
